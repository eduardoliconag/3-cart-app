import { Routes } from '@angular/router';
import { CartComponent } from './components/card-app/cart/cart.component';

export const routes: Routes = [
    {
        path: 'cart', 
        component: CartComponent
    }
];
